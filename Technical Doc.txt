Created by:  Rami Jaber

This is the technical document to get everything needed for the programs to run without problems:

Keep everything in the same directory to ensure programs and AI work properly:
Download the .zip file from:  https://github.com/ultralytics/ultralytics
extract into the directory 

pip install all the requirments in requirements.txt 
When the training code finishes running, a best.pt file will generate and is used to test the AI.


To use dragon.py:
Ensure the code is saved in the same directory as the image folder